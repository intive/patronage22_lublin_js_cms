import React from "react";
import AddCategoryForm from '../Modals/AddCategoryForm';

const Menu = () => {
  return (
    <section>
      <h1>Menu</h1>
      <AddCategoryForm />
    </section>
  );
};

export default Menu;
