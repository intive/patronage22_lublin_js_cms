import React from "react";

const Menu = () => {
  return (
    <section>
      <h1>Menu</h1>
    </section>
  );
};

export default Menu;

/*
<List>
      {[
        "Menu",
        "Home",
        "Strony",
        "Kategorie",
        "Produkty",
        "Zamówienia",
        "Klienci",
      ].map((text, index) => (
        <Link to="/products">
          <ListItem button key={text}>
            <ListItemText primary={text} />
          </ListItem>
        </Link>
      ))}
    </List>
 */
