import React from "react";
import Navigation from "../Navigation/Navigation";

const Menu = () => {
  return (
      <section>
        Menu
        <Navigation/>
      </section>
  );
};
export default Menu;
